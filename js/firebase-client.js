/*
  Firebase client initialization (Firestore) for the Book Consultation form.
  IMPORTANT:
  - Create a Firebase project
  - Enable Firestore
  - Create a web app and copy firebaseConfig
  - Paste firebaseConfig into the TODO section below
*/

// Import Firebase modules (works if you bundle with a build tool).
// If you're not bundling, use the Firebase CDN scripts instead.
// For a plain static site on Netlify, we use the CDN approach in book-consultation.html

export {};

