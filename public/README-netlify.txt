This folder is used only for Firebase Hosting deploys.

If you deploy to Netlify (recommended for this project), you do NOT need Firebase Hosting.
You only need the Firebase client config + Firestore rules + optional Cloud Function/email.

